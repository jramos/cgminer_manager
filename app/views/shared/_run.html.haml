%div
  = label_tag(:command, "Command:")
  = text_field_tag(:command)
%div
  = label_tag(:command, "Arguments:")
  = text_field_tag(:'arguments')

= submit_tag("Run")

%br
%br

%div
  See
  = link_to "cgminer_api_client README", "https://github.com/jramos/cgminer_api_client#commands"
  for available commands

#run-results
  - if @results
    - if @miner_id
      = render partial: 'shared/run/miner_results'
    - else
      = render partial: 'shared/run/manage_results'