#miner-pool
  %table(cellpadding="0" cellspacing="0")
    %thead
      %th Host
      %th Rate(5s)
      %th Rate(avg)
      %th Devices
    %tbody
      - @miner_pool.miners.each_with_index do |miner, miner_index|
        = render partial: 'shared/miner_summary_row', locals: { miner: miner, miner_index: miner_index }