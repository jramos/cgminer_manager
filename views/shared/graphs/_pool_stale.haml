- width  = target == 'miner' ? '850' : '864'
- metric = 'hashrate'
- url    = target == 'miner' ? "#{miner_url(@miner_host_port)}/graph_data/#{metric}" : "/graph_data/#{metric}"

#pool-stale-hashrate-graph
  %h2 Pool Stale %
  %canvas(id="pool_stale_hashrate" width="#{width}" height="400")

%script
  != "window.__poolStaleUrl = #{url.to_json};"
:javascript
  (function () {
    function run() {
      getJSON(window.__poolStaleUrl).then(function (hash_rate_response) {
        if (!hash_rate_response || hash_rate_response.length <= 1) {
          var el = document.getElementById('pool-stale-hashrate-graph');
          if (el) el.remove();
          window.__chartReady && window.__chartReady();
          return;
        }
        var labels = [], error_rates = [], error_rates_raw = [];
        hash_rate_response.forEach(function (response) {
          var date = new Date(response[0] * 1000);
          if (date.getMinutes() % 5 === 0) {
            var mm = '00' + date.getMinutes().toString();
            labels.push(date.getHours() + ':' + mm.slice(-2));
          } else {
            labels.push('');
          }
          error_rates.push(response[6] * 100);
          error_rates_raw.push(response[6]);
        });

        var canvas = document.getElementById('pool_stale_hashrate');
        var prior  = Chart.getChart(canvas); if (prior) prior.destroy();

        new Chart(canvas, {
          type: 'line',
          data: {
            labels: labels,
            datasets: [{
              label: 'Pool Stale',
              data: error_rates,
              backgroundColor: 'rgba(255, 221, 0, 0.25)',
              borderColor: '#d4b400',
              fill: false,
              tension: 0.3,
              pointRadius: 2
            }]
          },
          options: {
            responsive: true,
            animation: false,
            plugins: {
              legend: { display: true },
              tooltip: { callbacks: { label: function (ctx) { return ctx.parsed.y + '%'; } } }
            }
          }
        });

        injectHashrateTable([], [], error_rates_raw, '#pool-stale-hashrate-graph');
        window.__chartReady && window.__chartReady();
      }).catch(function () {
        window.__chartReady && window.__chartReady();
      });
    }
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', run);
    } else {
      run();
    }
  })();
